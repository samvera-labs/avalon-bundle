require 'rails_helper'

RSpec.describe "ActiveEncodeEncodeRecords", type: :request do
  describe "GET /active_encode_encode_records" do
    it "works! (now write some real specs)" do
      get active_encode_encode_records_path
      expect(response).to have_http_status(200)
    end
  end
end
